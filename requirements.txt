# Runtime dependencies. This mirrors [project.dependencies] in pyproject.toml,
# which is the authoritative source. Prefer `pip install -e .[dev]` for development.

numpy>=1.21          # array operations, gradients, linear algebra
PyYAML>=5.4          # YAML case-file parsing

# Optional, imported lazily only where needed:
#   scipy>=1.7       # sparse solvers in pyCoastal.physics.poisson  -> pip install pyCoastal[sparse]
#   vtk>=9.0         # VTK output in pyCoastal.io.write_vtk         -> pip install pyCoastal[vtk]
#   matplotlib>=3.4  # plotting in examples/                        -> pip install pyCoastal[plots]
